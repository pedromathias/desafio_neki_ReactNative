import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  
    container:{
        alignSelf:'center',
        alignItems:'center',
        width:110,
        height:130,
    
    },

    image:{
        resizeMode:'contain',
        width:90,
        height:90,
        borderRadius:30,
    }
})